# LyricFlow — Python Dependencies
# Install with: pip3.11 install -r requirements.txt

nemo_toolkit[asr]
google-generativeai
python-dotenv
soundfile
numpy
torch
requests

# Note: NeMo requires Python 3.11. The Hindi model (stt_hi_conformer_ctc_medium)
# downloads automatically on first run (~100MB, cached at ~/.cache/torch/NeMo/).
